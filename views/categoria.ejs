<div class="contenedor proximos-eventos">
  <h1><%= nombrePagina %></h1>
  <% if(meetis.length > 0){ %>
  <div class="grid columnas-md-3">
    <% meetis.forEach(meeti => { %> <%- include partials/card-meeti.ejs %> <% })
    %>
  </div>
  <!-- card -->

  <% } else { %>
  <p>No hay Meeti's en esta categoría</p>
  <% } %>
</div>
<!-- grid -->
